# 现场演示脚本 · AI 文案工坊

> 本项目为“图片小红书文案生成平台”新人实战考核项目。本文件用于现场演示前准备与演示当天照着走。

## 一、演示账号

| 角色 | 账号 | 密码 | 说明 |
| --- | --- | --- | --- |
| 普通用户 | `demo20260811` | `demo123456` | 演示工作台 / 生成 / 调优 / 历史记录 |
| 管理员 | `admin094040` | `Admin123456` | 演示后台管理台（本地库已有） |

> 若账号被删除或不存在，可在注册页重新注册普通用户；管理员需在 `.env` 配置 `ADMIN_CODE` 后用邀请码注册。

## 二、演示前准备（提前 10 分钟）

1. 确认三个服务在运行：
   - 前端：`http://localhost:5173`（注意用 localhost，不是 127.0.0.1）
   - 后端：`http://127.0.0.1:8000`
   - MySQL：Windows 服务 `MySQL80`
2. 准备 2~3 张演示图片（推荐 1 张产品图、1 张场景图），放在桌面或 U 盘。
3. 确认阿里云百炼 API Key 有额度（每次生成会消耗少量费用）。
4. 用演示账号登录一次，确认能进工作台。
5. 如果现场网络受限，提前用本机图片走“本地上传”流程，避免依赖外网图片 URL。

## 三、演示流程（约 8~10 分钟）

### 第 1 步：官网首页（约 1 分钟）
- 打开 `http://localhost:5173`，展示首页：产品介绍、功能亮点、真实笔记效果、FAQ。
- 讲解点：官网前台是加分项，视觉风格与工作台统一；支持暗色模式（右上角 🌙）。

### 第 2 步：登录 / 注册（约 1 分钟）
- 点击右上角“登录”，用演示账号 `demo20260811 / demo123456` 登录。
- 讲解点：JWT 鉴权，密码 PBKDF2 加盐哈希；未登录不能进工作台/历史/管理台。

### 第 3 步：生成文案（约 3 分钟）
- 进入工作台，选择“本地上传”或“在线图片 URL”。
- 演示前端校验：选一个非图片文件（如 txt），会立刻提示格式错误；超过 20MB 会提示大小超限。
- 填写可选信息：产品名称、目标人群；选择模型 `qwen-vl-plus`；选一个风格模板（如“活泼种草”）；创意度滑块调整温度。
- 点击“🚀 生成小红书文案”，展示流式逐字输出（SSE）。
- 生成后展示笔记卡片：标题、正文、标签、点赞/收藏/评论、一键复制、导出图片。
- 讲解点：标题字数提示（20 字以内），超过会警告；后端把记录写入本地 MySQL。

### 第 4 步：多轮调优 / 多版本（约 2 分钟）
- 在结果卡片输入修改意见（如“语气再活泼一点，标题缩短”），点击“生成调优版本”。
- 展示三个版本并排对比：点击版本卡片切换，当前版本高亮 + 对勾，下方展示完整卡片。
- 讲解点：多轮调优记录 `parent_id` 关联，支持多版本生成。

### 第 5 步：历史记录（约 1.5 分钟）
- 进入历史记录页，演示搜索、时间筛选（今天/近 7 天）、分页。
- 点开一条记录查看详情弹窗，演示复制、导出图片。
- 演示收藏 ⭐、仅看收藏筛选、删除（二次确认弹窗）。
- 讲解点：按用户隔离数据，收藏/删除只影响自己的记录。

### 第 6 步：后台管理台（约 1.5 分钟）
- 退出普通用户，登录管理员账号 `admin094040 / Admin123456`。
- 进入管理台：用量概览（统计数字动画 + ECharts 7 日折线图）、用户列表（头像/角色/注册时间/生成次数）、生成记录（缩略图/标题/标签/时间）。
- 讲解点：普通用户访问管理台会被拦截（403/路由守卫）。

## 四、备选讲解点（时间富余时）

- **架构**：Vue 3 + TypeScript 前端 → FastAPI 后端 → 阿里云百炼 Qwen-VL → 本地 MySQL，前端不直连模型/数据库。
- **流式输出**：后端 OpenAI 兼容 SSE，前端 `fetch + ReadableStream` 逐字渲染。
- **安全**：密钥全部在 `backend/.env`（已 gitignore），不硬编码；JWT 7 天有效期。
- **异常处理**：图片格式/大小、URL 非法、下载超时、模型失败、数据库失败均有明确提示。
- **部署**：Docker Compose 文件已提供（MySQL + 后端 + 前端 + Nginx）。

## 五、常见坑（演示前确认）

- 前端一定要用 `http://localhost:5173`，用 127.0.0.1 会连不上。
- PowerShell 里 npm 用 `npm.cmd`（执行策略会拦 `npm.ps1`）。
- 生成会真实调用阿里云 API 并产生费用，演示前确认 Key 有额度。
- 历史记录按登录账号隔离，换账号后看不到旧账号的记录。
- 不要在现场临时改代码；如需改动，改完跑 `npm run build` 确认能编译。
